# Cut-path entry candidates

`entries.ts` creates deterministic pierce, lead, tab and overcut candidates from transformed
nesting geometry. Candidates are immutable value objects and remain independent of machine-specific
post-processing.
